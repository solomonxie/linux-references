# Ansible

Playground for learning Ansible roles and playbooks.

Each role under `roles/` gets a matching `manual_<role>.sh` here showing
the same steps as plain shell commands, for reference without running
Ansible.

TODO: add the first role and its playbook.
